#!/usr/bin/make -f
#export DH_VERBOSE=1

include /usr/share/dpkg/buildflags.mk
CFLAGS	+= -Wall -pedantic
LDFLAGS += -Wl,--as-needed

override_dh_auto_build:
	$(MAKE) CC="gcc" CFLAGS="$(CFLAGS) $(LDFLAGS)"

%:
	dh $@

# End of file
